# typography-2-styles
TASK: encode the 26 spec styles as classes (Tailwind components/plugin) — inert until applied.
Resolve the dead text-2xs config while at it (it IS Detail/Base with a name).
CONTEXT: audit §"Recommended shape" ticket 2; design-system/docs/03-typography.md.
May ride in the same PR as typography-1.
DONE WHEN: one class binds all five properties per style; nothing visual changes yet.
DECIDED BY: orchestrator.
