# A-2026-08-02-orchestrator-answers
From the orchestrator, answering your last four reports' NEEDS:
1. BUTTONS TICKET: closed. Amended DONE WHEN: "all genuine Button/IconButton migrated;
   deliberate exceptions documented (avatar → Avatar extraction, brand chrome keeps palette)."
   The 35 radius-less tap targets are OUT OF SCOPE — separate "interactive regions" concern,
   parked with the §1 extraction decision. Move the ticket to done/ citing this file.
2. Mobile viewports in the ?preview harness: approved — add them in your next batch and
   re-verify gap fix #10 below `sm`.
3. §1 extraction (37 inline components): parked until typography-3 completes. Then you and
   Design propose an extraction order via peer mail (top candidates by reuse); orchestrator
   sequences from that. Don't start it unilaterally.
4. New TASK ticket alongside this file: button-control-styles.
