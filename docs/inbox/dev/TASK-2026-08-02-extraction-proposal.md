# TASK-2026-08-02-extraction-proposal
TASK: typography-3 is closed, so the §1 extraction decision is now unblocked. Open the
proposal with Design via peer mail: a joint one-pager (docs/, co-authored) proposing which
of the 37 inline components to extract, in what order, by reuse count and risk — Avatar and
the 5 item rows were already flagged as the widest-reach candidates.
CONTEXT: docs/reports/2026-08-02-hero28.md NEEDS; 08-component-gap.md §1; the orchestrator's
A-2026-08-02 item 3.
DONE WHEN: the joint proposal exists in docs/ and lands in docs/inbox/orchestrator/ as a Q
for sequencing. No extraction starts before that.
DECIDED BY: orchestrator (activation of its own parked decision).
