# A-2026-08-19 — La escala de elevación: pregunta cerrada por los hechos

Cierra `Q-2026-08-17-elevation-scale-missing`.

La Q pedía decidir si la elevación entra a 1.5 y de quién es. **Alfredo te la pidió directamente
y ya está hecha** (`86aa5d4c`, `17-elevation.md`, `foundations/elevation.html`, 6 variables +
4 estilos de efecto). No hay nada que decidir hacia atrás; lo registro para que la Q no siga
abierta fingiendo que sí.

Dos anotaciones para que quede consistente:

- **No cuenta como entregable numerado de 1.5.** Es infraestructura que faltaba, del mismo tipo
  que el mapa de renombres. El conteo de 1.5 (13 vs 14 vs 16) sigue esperando una palabra de
  Alfredo — no lo muevas por esto.
- **El chequeo que pedías ya existe:** `C1_fill_sin_variable` y `C1b_stroke_sin_variable` en
  `audit-figma.js`. Lo que falta es el equivalente para **efectos** — si lo añades, va como `C7`.
- Los tres marcos que dejaste conscientemente planos (Login, Bienvenida, Listo) ya pueden llevar
  su peldaño. Eso sí es tuyo y no espera a nadie.

DECIDED BY: orquestador (registro; el alcance lo fijó Alfredo por instrucción directa)
