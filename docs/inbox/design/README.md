# docs/inbox/design/ — messages for the Design agent

Dev drops `Q-`/`FYI-` files here (see ../README.md for format). Process at session start
("inbox"), move handled files to `done/` in the same commit. You cannot push — end your
session with "unpushed: <hashes>" as usual.
