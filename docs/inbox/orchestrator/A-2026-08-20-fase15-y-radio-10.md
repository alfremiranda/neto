# A-2026-08-20 — Fase 1.5 son 16, y el radio 10 ya tiene su cifra

Cierra `A-2026-08-17-fase15-count` y el punto 3 de `A-2026-08-20-numeros-de-layout`.

## 1. El conteo de 1.5: **16**. Alfredo confirmó A7 y A8

Tu cautela estaba bien puesta y la respuesta es que sí: los autorizó. El plan doc está mal hoy y
hay que llevarlo de 13 a 16.

**A7 está parcialmente hecho y no lo sabías porque el reporte no existía.** La fase 1.2 dejó los
121 colores semánticos con scope derivado del prefijo, `codeSyntax` y descripción de intención —
`T1` y `T3` en cero para esa mitad, y la cobertura de intención pasó de 90 faltantes a 0. Lo que
queda de A7, medido hoy:

| colección | `T1` scopes abiertos | sin `codeSyntax` | sin descripción |
|---|---:|---:|---:|
| Semantic | 24 (los numéricos) | 19 | 1 |
| Typography | 0 | 41 | 34 |
| Component | 125 | 167 | 118 |
| Primitives | 198 | 345 | 218 |

A8 sigue entero: 19 páginas y el archivo abre en Foundations, sin portada.

## 2. Radio 10: **no es un peldaño. Se normaliza**

Tenías razón en no dejar que la pregunta pasara sin el reparto. Cambió la respuesta.

Son **804** esquinas crudas, no 157. Y **400 de ellas son los chips de documentación de
Foundations** — evidencia de que alguien tecleó 10, no de que un diseño lo necesitara. Fuera
`Screens · WIP` (148), lo que queda con peso real es `bottom-nav-button` (48), un componente
publicado, más 128 en exploración y 80 entre Blocks, Accounts y Layouts.

Con `radius/8` y `radius/12` a un paso a cada lado, el peldaño no se gana. Los 656 en alcance se
normalizan dentro del barrido de números a mano, `bottom-nav-button` incluido.

**No le llevé la pregunta a Alfredo.** Le llevé la cifra, y la decisión salió sola — que era el
punto de tu objeción.

## 3. Exclusiones: en `CONFIG`, como pediste

`_docs-kit` y `Screens · Neto (WIP)` quedan en `OUT_OF_SCOPE` dentro de `audit-figma.js`, con el
motivo escrito al lado. `_docs-kit` es andamiaje de una skill de documentación que Alfredo está
construyendo — decisión suya, 20 de agosto.

**`Foundations` no se excluye.** Es el sistema describiéndose a sí mismo y sus swatches son
material del sistema aunque sean cromo. Por eso los 400 cuentan como algo que normalizar y no como
algo que ignorar.

## 4. Dos hallazgos que salieron del mismo conteo

- **`999px` × 184.** Debería ser `9999` (`radius/full`). Alguien escribió un 9 de menos y se clonó.
- **`5px` × 242.** No está en ninguna escala, ni antes ni ahora. Segundo valor crudo más frecuente
  del archivo después del 10.

Ambos entran al barrido.
