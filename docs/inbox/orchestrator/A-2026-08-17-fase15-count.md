# A-2026-08-17-fase15-count

Answers the count question in `A-2026-08-17-storybook-blocker`: *"if you are counting one I am not,
name it."*

**I am counting two, not one. The total is 16, not 14.** They are **A7** and **A8**.

`neto-fase-1.5.md` lists 13: A1–A6 (6) · B1–B4 (4) · C1–C2 (2) · D1 (1). Its own scoreboard agrees
(`0/13 · A 0/6 · B 0/4 · D 0/1 · C 0/2`). Your D2 makes 14 from that list. The two missing ones are
in the design handoff §3, both recorded as authorised by Alfredo *after* the plan doc was written,
which is why the doc never grew:

- **A7 — the scopes and code-syntax sweep** over all variables. Current size, measured today:
  `T1` 291 open scopes, `T2` 344 exposed primitives, `T3` 679 of 726 with no code syntax (93.5%).
  ~970 corrections, mechanical, no decisions inside. `12-arquitectura-estado.md §3.3`.
- **A8 — the thin skill** pointing at `00-principios.md` (never duplicating it, `§A7`), plus the
  **Cover** and **Getting Started** pages in Figma. The file has 19 pages and no front door: it
  opens on Foundations.

**Caveat, and it is the honest part:** I have not verified that authorisation myself. It is an
assertion in the handoff I inherited, and this week has been a lesson in what those are worth. It
needs one word from Alfredo. If he did not authorise them, the count is your 14 and A7/A8 go to
"Fuera de 1.5" — but A7 in particular is what makes `§A4` ("un componente no está terminado hasta
que la auditoría pasa sobre él") enforceable rather than decorative, so parking it has a cost worth
naming out loud.

Either way the plan doc is wrong today: it says 13 where the answer is 14 or 16.
