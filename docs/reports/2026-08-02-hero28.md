# 2026-08-02 — Amount/Hero at 28: collision resolved

Screenshot: `2026-08-02-hero28-resolved.png`.

DID:
- Pulled and rebuilt. `Amount/Hero` is **28/28 SemiBold**. Verified on both affected views:

| View | Section | Hero | |
|---|---|---|---|
| Cuentas | 24/32 | **28/28** | 6 figures, all above the title |
| Resumen | 24/32 | **28/28** | 3 figures |

  Order on a shell screen is now Hero 28 > Section 24 > Subsection 20. The figures win outright
  instead of tying and winning on repetition.
- **Checked the no-new-collision claim empirically rather than accepting it.** Design's reasoning
  was that `Heading/Display` (28/36 Bold +0.5) never appears inside the app shell, so its shared
  28 with Hero cannot collide. Counted the rendered DOM across the shell views: **0 `Display`
  elements**. The claim holds, and now it holds by measurement rather than by rule — which is the
  same standard I asked of the previous one.
- 28/28 rather than 28/32 is deliberate on Design's side: Hero's line height has always equalled
  its size, money has no descenders, and the KPI card stays compact. Only the size moved.

FOUND:
- Nothing new. This is the first exchange today that closed without turning up a second problem.

DECISIONS:
- Applied nothing myself beyond the pull. The lever was Design's token and they moved it; my part
  was the measurement that showed it needed moving, and the measurement that confirms it worked.

NEEDS:
- Nothing. `typography-3` is closed, its last open question is answered, and the dev inbox is
  empty.
- Still parked and now unblocked: the `§1` extraction decision — 37 components that exist in
  Figma and in code only as inline markup. The orchestrator asked that Design and I propose an
  order via peer mail once typography-3 completed. It has.
