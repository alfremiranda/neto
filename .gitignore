# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.vite

# Claude Code — settings.json is shared, settings.local.json is per-machine.
# Needed here (not only in a personal global ignore) so every agent working on
# this tree sees the same thing. Note `*.local` above does not match it.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Files this environment cannot delete (device VM forbids unlink). Safe to remove by hand.
**/_to_delete/
storybook-static/
test/visual-diff/
dist-motion/
